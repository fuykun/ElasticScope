# ============================================
# ElasticScope Environment Configuration
# ============================================
# Copy this file to .env and configure your settings

# ==================== SERVER ====================
NODE_ENV=development
PORT=3001

# ==================== SECURITY ====================
# Password encryption key (REQUIRED in production!)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=elasticscope-default-key-change-me!

# ==================== DATABASE ====================
# Database type: sqlite | postgresql | mysql
# Default: sqlite (no additional configuration needed)
DB_TYPE=sqlite

# ---------- SQLite Configuration ----------
# Only used when DB_TYPE=sqlite
# Path to SQLite database file (default: ./data/connections.db)
# DB_PATH=./data/connections.db

# ---------- PostgreSQL Configuration ----------
# Only used when DB_TYPE=postgresql
# All fields are REQUIRED when using PostgreSQL
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=elasticscope
# DB_USER=postgres
# DB_PASSWORD=your_password_here
# DB_SSL=false
# DB_POOL_SIZE=10

# ---------- MySQL Configuration ----------
# Only used when DB_TYPE=mysql
# All fields are REQUIRED when using MySQL
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=elasticscope
# DB_USER=root
# DB_PASSWORD=your_password_here
# DB_SSL=false
# DB_POOL_SIZE=10
